import { gql } from 'graphql-request';
import { IssuedQueryParams } from '../../src/types';
import { createGQLWhereString, createGQLBlockNumberString } from '../../src/utils';

export const createDailyIssuedQuery = (params?: IssuedQueryParams): string => {
	const whereString = createGQLWhereString(
		Object.entries({
			account: (params?.account ?? null) != null ? 'account' : null,
			block_gte: (params?.minBlock ?? null) != null ? 'minBlock' : null,
			timestamp_lt:
				(params?.autoGeneratedPaginationField ?? null) != null
					? 'autoGeneratedPaginationField'
					: null,
		})
	);

	return gql`
		query dailyIssueds($autoGeneratedPaginationField: Int, $minBlock: Int, $max: Int, $account: String) {
			dailyIssueds(
				first: $max${createGQLBlockNumberString(params?.blockNumber ?? null)}
				where: ${whereString}
				orderBy: timestamp
				orderDirection: desc
			) {
        id
        account
        value
        source
        timestamp
        gasPrice
        block
			}
		}
	`;
};
