# This workflow uses actions that are not certified by GitHub.
# They are provided by a third-party and are governed by
# separate terms of service, privacy policy, and support
# documentation.

# This workflow will install Deno and run tests across stable and canary builds on Windows, Ubuntu and macOS.
# For more information see: https://github.com/denoland/setup-deno

name: Publish Library

on:
  repository_dispatch:
    types: update-dependency

jobs:
  # lints:
  #   name: Run Lints
  #   runs-on: ubuntu-latest

  #   steps:
  #     - uses: actions/checkout@v2
  #     - uses: actions/setup-node@v2
  #       with:
  #         node-version: '14'
  #     - run: npm ci
  #     - run: npm run bootstrap
  #     - run: npm run lint
  #     - run: npm run build
  #     - run: npm run test-all
  update_version:
    name: Update contracts-interface
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v2
        with:
          node-version: '14'
      - run: 'echo "Update @synthetixio/contracts-interface with synthetix@${{ toJson(github.event.client_payload.version) }}"'
      - run: cd packages/contracts-interface && "npm install@${{ toJson(github.event.client_payload.version) }}"
