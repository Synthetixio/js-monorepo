type Pool @entity {
  id: ID!
  created_at_block: BigInt!
  owner: Bytes!
  created_at: BigInt!
  "Only set when name of the pool got updated"
  sender: Bytes
  "Only set when name of the pool got updated"
  name: String
  "Only set when the pool got updated"
  updated_at: BigInt
  "Only set when the pool got updated"
  updated_at_block: BigInt
}
