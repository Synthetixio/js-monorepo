{
  "name": "@synthetix/subgraphs-v3",
  "license": "MIT",
  "private": true,
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ fritzschiff/subgraphs",
    "create-local": "graph create --node http://localhost:8020/ fritzschiff/subgraphs",
    "remove-local": "graph remove --node http://localhost:8020/ fritzschiff/subgraphs",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 fritzschiff/subgraphs",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.35.0",
    "@graphprotocol/graph-ts": "0.28.1"
  },
  "devDependencies": {
    "matchstick-as": "0.5.0"
  },
  "depcheck": {
    "ignorePatterns": [
      "dist"
    ],
    "ignoreMatches": [
      "webpack-dev-server"
    ]
  }
}
