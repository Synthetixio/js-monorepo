{
	"name": "js-monorepo",
	"workspaces": {
		"packages": [
			"packages/providers",
			"packages/contracts-interface",
			"packages/optimism-networks",
			"packages/transaction-notifier"
		]
	},
	"scripts": {
		"test": "jest --forceExit --coverage --no-cache --runInBand",
		"lint": "eslint './packages/**/*/src/**/*.{js,ts,tsx}' && tsc"
	}
}
